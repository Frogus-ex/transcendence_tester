#Dependencies needed (latest stable version, currently using Python 3.14.6)

# Websockets
websockets==17.0.1

# Database (Redis & PostgreSQL)
redis==6.4.0
psycopg[binary, pool]

# ORM for database (SQLAlchemy)
sqlalchemy>=2.0.0

# Celery worker
celery[redis]>=5.6.0